<footer class="main-footer bg-dark text-light py-6">
    <div class="row align-items-center">
        <div class="col-12 col-md-6 text-center text-md-left">
          <p>&copy; <script>document.write(new Date().getFullYear())</script> Blog Posts for Days. All rights reserved.</p>
        </div>
    </div>
</footer>
  
<script src="./js/app.js"></script>
<script>
    document.getElementById("image").addEventListener("change", function (e) {
      const file = e.target.files[0]; 
      if (file) {
        const reader = new FileReader();
        
        reader.onload = function (event) {
          const imagePreview = document.getElementById("image-preview");
          imagePreview.innerHTML = `<img src="${event.target.result}" alt="Image preview" class="img-fluid" />`;
        };
        
       reader.readAsDataURL(file);
      }
    });
  </script>
  
</body>
</html>
  